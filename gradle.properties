org.gradle.caching=true
org.gradle.configuration-cache=true
org.gradle.parallel=true

# SonarCloud project properties.
# Override locally in ~/.gradle/gradle.properties — never commit actual values here.
# sonar.projectKey=alzheimer-assistant
# sonar.organization=your-sonarcloud-org
#
# Pass the token at the command line or via environment variable:
#   ./gradlew sonar -Dsonar.token=$SONAR_TOKEN
#   SONAR_TOKEN=... ./gradlew frontSonar
